package aula02;

import java.util.Scanner;

public class Ex02 {
    public static void main(String[] args) {
        //int num;
       // String texto;
        Scanner ler = new Scanner(System.in);

        //System.out.print("Digite um numero: ");
        //num = ler.nextInt();
        
        //ler em branco
        //ler.nextLine();   //vai ler o enter
        
       // System.out.print("Texto: ");
       // texto = ler.nextLine();

       // System.out.println("O numero lido: "+num);
       // System.out.println("o texto lido: "+texto);

    
        //short numShort = ler.nextShort();
        //long numLong = ler.nextLong();
        //float numFloat = ler.nextFloat();
        //double numDouble = ler.nextDouble();
        
        //não existe um ler char
        //char letra = ler.nextChar();
        
        //para ler letra, temos que ler um texto e tirar a primeira letra (posição: 0)
        //char letra = ler.nextLine().charAt(0);
       
    }
}
